// Function to get an array of property names from an object
function getPropertyNames(obj) {
    // Use Object.keys() to get an array of keys
    return Object.keys(obj);
  }
  
  // Example usage
  const person = {
    name: "John",
    age: 30,
    city: "New York"
  };
  
  console.log(getProperty)